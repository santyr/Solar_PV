[project]
name = "earthship-energy-analytics"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["psycopg2>=2.9"]

[project.scripts]
energy-data = "earthship_energy.cli:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
